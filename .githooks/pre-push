#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

# Run local gate before push (skip with SKIP_FACTORY_GATE=1)
if [[ "${SKIP_FACTORY_GATE:-0}" == "1" ]]; then
  echo "[pre-push] SKIP_FACTORY_GATE=1 -> skipping"
  exit 0
fi

./scripts/pr_gate_local.sh
