#!/usr/bin/env bash
set -euo pipefail

BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [[ "$BRANCH" == "main" && "${GATEKEEPER_APPROVED:-0}" != "1" ]]; then
  echo "[pre-merge-commit] DENY: merging to main requires GATEKEEPER_APPROVED=1"
  exit 1
fi
