# Alpha Status API (Starter)

Run service:

```bash
python3 status_service.py
```

Check endpoint:

```bash
curl -s http://127.0.0.1:8080/status
```

Run tests:

```bash
python3 -m unittest test_status_service.py
```
